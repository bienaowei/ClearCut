# ClearCut 开发计划

> 基于《ClearCut 开发提示词》拆解的分阶段实施计划。目标：纯前端浏览器图片抠图工具，输出透明背景 PNG。

## 一、总体策略

按"地基 → 通用能力 → 三种模式 → 优化"的顺序推进，保证每个阶段结束都有可运行的产物：

1. **地基先行**：脚手架 + 类型 + store 骨架，先让空画布跑起来。
2. **通用能力优先**：图片加载、缩放平移、棋盘格、撤销/重做框架——三种模式都依赖它们。
3. **模式分批落地**：画笔擦除 → 多边形裁剪 → 多边形保留，复用程度递增。
4. **最后做优化**：大图降采样、Web Worker 打包、快捷键完善。

里程碑划分见第三节，预计 7 个阶段。

---

## 二、技术决策确认

| 关注点 | 决策 |
| --- | --- |
| 包管理 | **pnpm**（统一使用，禁止混用 npm/yarn 以免 lockfile 冲突） |
| 构建 | Vite + React 18 + TypeScript |
| 画布 | react-konva 渲染交互层，原生 `<canvas>`（离屏）负责像素级遮罩与导出 |
| 状态 | Zustand 三个 store（editor / history / crop），Immer 生成快照 |
| Konva 节点 | 一律 `useRef` 持有，**不进** React state（提示词注意事项 1） |
| 坐标系 | 多边形顶点、画笔遮罩**统一存原图像素坐标**，显示时乘缩放比 |
| 导出 | 独立离屏 Canvas，不复用 Konva Stage |
| 打包 | JSZip 在 Web Worker 中执行，file-saver 触发下载 |

---

## 三、分阶段任务清单

> **当前进度（2026-06-15）**：阶段 0–5 ✅ 全部完成并通过端到端验证（`tsc` / `vite build` 通过，预览实测：图片加载、三模式切换、多边形绘制裁剪、Web Worker zip 导出均正常，控制台零报错）。阶段 6 核心已完成，「大图 >4096px 降采样」按需求暂缓（当前场景涉及不到）。图例：`[x]` 完成 · `[~]` 部分完成 · `[-]` 暂缓/不做 · `[ ]` 未开始。

### 阶段 0：项目脚手架（地基）
- [x] Vite + React + TS 模板（手动初始化）
- [x] 安装依赖（pnpm）：`react-konva konva zustand immer jszip file-saver`
- [x] 常用命令：`pnpm dev` / `pnpm build` / `pnpm preview`
- [x] 配置目录结构（按提示词 `src/` 树）
- [x] 全局主题 CSS 变量（暖色暗调配色表，见提示词主题风格）
- [x] `types/index.ts`：`Point` / `PolygonRegion` / `ExportConfig` / `CropResult` / `EditorMode` / `EditorState`
- [x] App.tsx 布局骨架：顶部工具栏 / 中间画布 / 右侧面板 / 底部状态栏
- **产物**：可运行的空壳，四区布局到位

### 阶段 1：状态管理骨架
- [x] `editorStore.ts`：mode、image、zoom、offset、画笔参数、exportConfig
- [x] `historyStore.ts`：undo/redo 栈（上限 30），`pushSnapshot / undo / redo`，泛型化以承载不同操作类型
- [x] `cropStore.ts`：polygons 数组、activePolygonId、增删改、拖拽排序
- [x] 设计快照粒度：画笔存遮罩 ImageData/dataURL，多边形存 polygons 结构（Immer produce）
- **产物**：store 单测可通过，控制台可驱动状态

### 阶段 2：通用画布能力
- [x] `EditorCanvas.tsx`：Konva Stage + 自适应容器尺寸
- [x] 棋盘格背景层（`#2e2222` / `#382a2a` 交替）
- [x] `ImageLayer.tsx`：原图显示，加载后自适应缩放到视口
- [x] 图片加载三通道：按钮选择文件 / 拖拽到画布 / Ctrl+V 粘贴（jpg/png/webp/bmp）
- [x] `useCanvasZoom.ts`：滚轮以光标为中心缩放、空格+拖拽平移
- [x] `StatusBar.tsx`：缩放比例、光标坐标、图片尺寸
- **产物**：可加载图片、缩放、平移，状态栏实时更新

### 阶段 3：模式一 — 画笔擦除
- [x] `useBrush.ts`：离屏 alpha 遮罩 Canvas，`destination-out` 绘制
- [x] 鼠标按下→移动→抬起一次笔画；移动采样 + **路径插值补点**（间距 > 半径时补点）
- [x] `BrushLayer.tsx`：遮罩合成显示、跟随光标的画笔预览圈（辅助色 `#f0a868`）
- [x] `BrushPanel.tsx`：大小滑块 1–100px（实时预览光标大小）、硬度滑块（边缘羽化）
- [x] 撤销/重做：每次 mouseup 存遮罩快照，接入 historyStore
- [x] 导出：原图 + 遮罩合成透明 PNG，可选自动裁剪到内容包围盒
- **产物**：完整可用的橡皮擦模式

### 阶段 4：模式二 — 多边形裁剪
- [x] `usePolygon.ts`：单击加顶点、预览线段、双击/点起点闭合、ESC 取消
- [x] `polygonMath.ts`：包围盒、点是否在多边形内、边线中点
- [x] `PolygonLayer.tsx`：闭合多边形半透明填充（主色 `#e8453c`）、自动配色
- [x] 编辑交互：顶点拖拽手柄、整体拖拽、Delete 删除、双击边线插点、右键删点（≥3 顶点保留）
- [x] `useExport.ts`：每个多边形 clip + drawImage 到目标尺寸离屏 Canvas
- [x] 尺寸配置三模式：A 固定尺寸（等比缩放居中）/ B 自适应统一（最大包围盒）/ C 原始尺寸；`imageSmoothingQuality: 'high'`
- [x] `CropListPanel.tsx`：实时缩略图、名称/尺寸、点击高亮、拖拽排序
- [x] 批量导出：JSZip 打包 `{序号}_{名称}.png` + `manifest.json`（名称、原始坐标、导出尺寸）；单个导出
- **产物**：完整可用的批量裁剪模式

### 阶段 5：模式三 — 多边形保留
- [x] 复用 `usePolygon` 绘制/编辑逻辑，限制为单个多边形（新建替换旧的）
- [x] `PreviewOverlay.tsx`：多边形外部叠加半透明遮罩，实时预览反向裁剪效果
- [x] `RetainPanel.tsx`：三种导出选项（保留原图尺寸 / 裁剪到包围盒 / 指定尺寸）
- [x] 导出：内部保留、外部透明
- **产物**：完整可用的反向裁剪模式

### 阶段 6：优化与收尾
- [-] 大图优化：> 4096px 降采样显示，导出仍用原图（**暂不做**：当前场景涉及不到超大图）
- [x] Web Worker 执行 zip 打包，避免阻塞 UI
- [x] `Toolbar.tsx` 完整化：模式切换、加载、撤销/重做、导出按钮联动
- [x] `ShortcutHelp.tsx`：快捷键说明弹窗（Ctrl+Z / Ctrl+Shift+Z / ESC / Delete / 空格 / Ctrl+V）
- [x] 全局撤销/重做栈跨模式打通校验
- [~] 边界测试：空多边形、单点多边形、跨模式切换数据保留（已做端到端冒烟测试；超大图项暂缓）
- **产物**：达到提示词全部验收点的可发布版本

---

## 四、关键风险与对策

| 风险 | 对策 |
| --- | --- |
| 快速移动画笔断线 | mousemove 间距 > 画笔半径时沿路径插值补点 |
| 缩放下坐标精度丢失 | 顶点/遮罩存原图像素坐标，仅显示时乘缩放比 |
| 大图性能卡顿 | 显示层降采样，导出用原图离屏 Canvas |
| zip 打包阻塞 UI | 移入 Web Worker |
| Konva 节点进 state 导致性能问题 | 全部 useRef 持有 |
| 撤销快照内存膨胀 | 栈上限 30，画笔快照用压缩 dataURL 或差分 |

---

## 五、建议实施顺序

阶段 0 → 1 → 2 为不可跳过的串行地基。阶段 3/4/5 三种模式相对独立，但建议按 **3 → 4 → 5** 顺序（4 与 5 共享多边形逻辑，先做 4 再抽出复用给 5）。阶段 6 贯穿后期，部分项（如快捷键弹窗、工具栏联动）可在对应模式完成后增量补齐。

每个阶段结束应保证 `pnpm dev` 可运行且无报错，便于增量验证。
