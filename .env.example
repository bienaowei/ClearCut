# 环境变量示例。复制需要的项到 .env.production（生产构建生效）或 .env.local（本地覆盖）。

# 大资源 CDN 根地址（结构与 public/ 一致，末尾保留斜杠）。
# 配置后：ORT wasm / LaMa 权重本地缺失时自动从此 CDN 拉取。SAM 始终走本地。
VITE_ASSET_CDN=https://你的域名/ClearCut/

# 单独覆盖某一项（完整 URL，优先级高于 VITE_ASSET_CDN），按需启用：
# VITE_ORT_WASM_URL=https://你的域名/ClearCut/ort/ort-wasm-simd-threaded.jsep.wasm
# VITE_LAMA_MODEL_URL=https://你的域名/ClearCut/models/lama/lama_fp32.onnx

# SAM 模型来源：local（默认，本地自托管）/ remote（HF 官方）/ mirror（hf-mirror 镜像）
# VITE_SAM_MODEL_SOURCE=local
