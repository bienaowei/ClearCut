# 环境变量示例。本项目的 .env.local / .env.production 已纳入 git（均为 VITE_ 公开变量，无密钥）。

# LaMa 权重来源（208MB，不入库）。指向 HuggingFace 官方，不绑卡、零托管。
# 优先级最高，跳过本地/CDN 探测。这是当前线上采用的方式。
# 注意：必须用官方 huggingface.co —— hf-mirror 是 308 跳转、重定向响应不带
# Access-Control-Allow-Origin，浏览器在 COEP 下会拦（CORS 链每跳都要带 ACAO）。
VITE_LAMA_MODEL_URL=https://huggingface.co/Carve/LaMa-ONNX/resolve/main/lama_fp32.onnx

# 以下为可选/旧方案，按需启用：

# 大资源 CDN 根地址（结构与 public/ 一致，末尾保留斜杠）。配置后 ORT wasm / LaMa
# 本地缺失时从此 CDN 拉取。当前 ORT wasm 随站点同源部署，一般无需配置。
# VITE_ASSET_CDN=https://你的域名/ClearCut/

# 单独覆盖 ORT wasm（完整 URL）：
# VITE_ORT_WASM_URL=https://你的域名/ClearCut/ort/ort-wasm-simd-threaded.jsep.wasm

# SAM 模型来源：local（默认，本地自托管）/ remote（HF 官方）/ mirror（hf-mirror 镜像）
# VITE_SAM_MODEL_SOURCE=local
