# ClearCut UI 美化方案

> **状态（2026-06-15）**：阶段 A–G 全部已实施 ✅。`tsc` / `vite build` 通过，预览实测无报错，视觉确认（渐变工具栏 + 红色 segmented 模式切换、全局内联 SVG 图标、卡片化侧边栏、引导式空状态、图标状态栏）。图标改用**自包含内联 SVG**（见 `src/components/common/Icon.tsx`），未引入外部图标库——因镜像 registry 的 `lucide-react` latest 是过时的 1.18.0。

> 目标：在保留「暖色暗调」主题的前提下，把当前偏「功能原型」的界面，提升到专业图像工具（Figma / Photopea 暗色档次）的视觉质感。不改任何功能逻辑，只动样式层与图标。

---

## 一、当前问题诊断

| 区域 | 现状问题 |
| --- | --- |
| 整体 | 纯平面、无层次/景深；圆角、间距、阴影没有统一 token，靠零散数值堆砌 |
| 工具栏 | 按钮全是文字、分组靠 gap 区分不明显；模式切换是三个独立按钮，缺「当前态」的连贯感 |
| 图标 | 用的是 emoji（✂ ↶ ⬇ ✕ ⌨），跨系统渲染不一致、不专业 |
| 按钮 | 尺寸不统一、hover 只有边框变色、主按钮是纯色块缺质感；无 focus ring |
| 侧边栏 | 字段平铺无分组卡片；原生 `range` 滑块丑；数字输入无单位、无步进 |
| 裁剪列表 | 卡片扁平、激活态只有边框；拖拽排序无视觉反馈；缩略图棋盘格偏粗 |
| 画布 | 棋盘格 20px 偏大显脏；空状态是两行纯文字，缺引导感；拖拽高亮是硬虚线 |
| 状态栏 | 文字 + `·` 分隔，信息层级弱；数字非等宽，跳动 |
| 字体 | 无 type scale，字号/行高/字重随手写 |

---

## 二、设计令牌（第一步，地基）

在 `index.css` 扩充 CSS 变量，后续所有组件改用 token，杜绝魔法数字：

```css
:root {
  /* 圆角 */
  --r-sm: 6px;  --r-md: 9px;  --r-lg: 14px;  --r-pill: 999px;
  /* 阴影（暖色调，非纯黑，避免发灰） */
  --shadow-1: 0 1px 2px rgba(0,0,0,.25);
  --shadow-2: 0 4px 14px rgba(0,0,0,.35);
  --shadow-pop: 0 12px 40px rgba(0,0,0,.5);
  --glow-primary: 0 0 0 3px rgba(232,69,60,.25);   /* focus ring */
  /* 间距步进 */
  --sp-1:4px; --sp-2:8px; --sp-3:12px; --sp-4:16px; --sp-5:24px;
  /* 过渡 */
  --ease: cubic-bezier(.2,.7,.3,1);
  --dur: .16s;
  /* 表面分层（在 panel 基础上再分两层，制造景深） */
  --surface-1: #2f2222;
  --surface-2: #3b2a2a;
  --surface-hover: #45302f;
  /* 主色渐变（按钮质感） */
  --primary-grad: linear-gradient(180deg, #ff5c52 0%, #e8453c 100%);
}
```

字体 type scale：`--fs-xs:11px / --fs-sm:12px / --fs-base:13px / --fs-md:15px / --fs-lg:17px`；数值显示统一 `font-variant-numeric: tabular-nums`，状态栏不再抖动。

---

## 三、分阶段实施

### 阶段 A — 令牌 + 基础控件（收益最大，优先做）
- [x] 注入第二节的设计令牌
- [x] 重构 `button`：统一高度（28/32px 两档）、`--r-md`、hover 抬升 `--surface-hover`、`:active` 轻微下压、`:focus-visible` 加 `--glow-primary`
- [x] 主按钮改 `--primary-grad` + `--shadow-1`，hover 提亮
- [x] 自定义 `input[type=range]`：细 track（4px，已填充段用主色）、圆形 thumb（带阴影、hover 放大）
- [x] 数字输入加单位后缀（px）与 hover/focus 态
- **产物**：所有交互控件统一、有微反馈

### 阶段 B — 图标系统
- [x] 引入 `lucide-react`（轻量、暗色友好），替换所有 emoji
  - 模式：画笔=`Brush`、裁剪=`Scissors`、保留=`Lasso`/`Hexagon`
  - 操作：撤销=`Undo2`、重做=`Redo2`、加载=`ImagePlus`、导出=`Download`、删除=`Trash2`、快捷键=`Keyboard`、关闭=`X`
- [x] 图标 + 文字按钮统一对齐（icon 16px，gap `--sp-2`）
- **产物**：跨平台一致的专业图标

### 阶段 C — 工具栏重构
- [x] 模式切换做成 **segmented control**：一个容器，激活项用主色背景 + 阴影，切换有滑动/淡入动画
- [x] 分组之间用 1px 渐隐竖分隔线（而非纯 gap）
- [x] Logo 区加品牌色剪刀图标 + 字距微调
- [x] 工具栏整体加 `--shadow-1` 与底部高光线，强化「悬浮于画布之上」
- **产物**：顶部栏有信息层级和品牌感

### 阶段 D — 侧边栏面板
- [x] 字段按语义分「卡片组」（`--surface-1` 背景 + `--r-md` + 内边距），组间留白
- [x] 标题区加小图标 + 一句灰色副标题（已有 hint，统一样式）
- [x] segmented 控件复用到尺寸模式/导出方式选择
- [x] 滑块行：左标签 + 右数值徽标（等宽数字）
- **产物**：参数面板清晰、有呼吸感

### 阶段 E — 裁剪列表
- [x] 卡片悬浮阴影、hover 抬升、激活态左侧 3px 主色竖条 + 轻微高亮背景
- [x] 缩略图棋盘格缩到 8px、加 `--r-sm` 与内描边
- [x] 拖拽排序：被拖项半透明 + 目标位插入线指示
- [x] 行内按钮（导出/删除）默认弱显示，hover 卡片时显现
- **产物**：列表精致、拖拽反馈明确

### 阶段 F — 画布与空状态
- [x] 棋盘格缩到 ~14px 并降低明暗对比，更干净
- [x] 空状态做成居中引导卡：大号图标 + 主标题 + 副说明 + 「选择图片」主按钮（替代纯文字）
- [x] 拖拽悬停：整画布柔和发光边框 + 半透明遮罩提示「松开以载入」
- **产物**：首屏更有引导性、不空洞

### 阶段 G — 状态栏与收尾
- [x] 每项加 16px 弱色图标（缩放/坐标/尺寸），分隔改细竖线
- [x] 数值等宽、定宽容器防抖动
- [x] 全局 `:focus-visible`、滚动条已美化项复核
- [x] 模式切换 / 面板切换加 120ms 淡入
- **产物**：细节统一，达发布观感

---

## 四、优先级建议

**最高性价比顺序：A → B → C**（令牌 + 控件 + 图标 + 工具栏，改完观感已大幅提升）。
D/E 紧随其后（用户操作最久的区域）。F/G 为锦上添花。

每个阶段都是纯样式/图标改动，**不触碰 store、hooks、canvas 逻辑**，可逐阶段验证、随时停手。

## 五、新增依赖

- ~~`lucide-react`~~ → **改为零依赖**：自建 `src/components/common/Icon.tsx`（内联 SVG，Lucide 风格描边）。原因：当前镜像 registry 的 `lucide-react@latest` 解析为过时的 1.18.0，图标名/API 与现代版不符，故弃用外部库。

---

## 六、风险与边界

- Konva 画布内的视觉（多边形描边色、画笔预览圈）已用主题色，本次主要美化 **DOM/CSS 外壳**，画布内只微调棋盘格与颜色常量。
- 不改任何交互行为与导出结果，回归风险低。
