# 冷色调（白蓝）主题接入方案

> 目标：在保留现有暖色暗调主题的前提下，新增一套以**白底蓝调**为主的冷色调亮色主题，用户可手动切换。

---

## 一、技术选型结论

**沿用现有 CSS 变量方案，不引入 Tailwind。**

理由：
1. `src/index.css` 已经把全部颜色抽成约 20 个 CSS 变量（`--bg` / `--panel` / `--primary` / `--surface-*` 等），整个项目两个 CSS 文件 848 行，`var(--xxx)` 引用 **203 处**，组件层基本无硬编码颜色 —— 设计令牌体系已经成形。
2. Tailwind 的主要价值（原子化 token）当前体系已经具备；切换到 Tailwind 需要改写全部 className，工作量是新增主题的几十倍，性价比低。
3. 暗/亮主题切换是 CSS 变量最经典的应用场景，用一行 `:root[data-theme='light']` 选择器即可覆盖。

---

## 二、改动范围盘点

### 2.1 已用变量的部分（零改动）

`src/App.css` 与 `src/index.css` 中所有 `var(--xxx)` 引用 —— 共 203 处 —— 新主题自动生效，不需要改。

### 2.2 硬编码颜色需修复点

| 文件 | 行号 | 当前值 | 处理方式 |
|---|---|---|---|
| `src/index.css` | 102, 177 | `#5a3a39`（暗调边框 hover） | 改为 `var(--border-hover)`，新增该变量 |
| `src/index.css` | 207, 228 | `#2a1f1f`（滑块 thumb 边框） | 改为 `var(--bg)` |
| `src/index.css` | 144, 151 | `#d8453a` / `#e2554a` / `#cf4133`（danger 渐变） | 抽成 `--danger-grad` / `--danger-grad-hover` 变量 |
| `src/App.css` | 14 | `#3b2a2a` 渐变起点 | 改为 `var(--surface-2)` |
| `src/App.css` | 538 | `#ff6d63` | 抽成 `--primary-light` 变量 |
| `src/App.css` | 81, 450 | `#fff` | 保留（按钮文字白色在两种主题下均成立） |

### 2.3 Konva 画布层（需主题感知）

`react-konva` 不直接读 CSS 变量，需要从 JS 侧拿到当前主题色。两个文件涉及：

- `src/components/Canvas/BrushLayer.tsx` —— `#ff7a45`（笔刷描边/填充）
- `src/components/Canvas/PolygonLayer.tsx` —— `#e8453c`、`#f0a868`、`#2a1f1f`、`#fff`、`#e8453c22`（多边形选区、锚点）

**处理方式**：新增 `useThemeColors()` hook，从 store 读取当前主题，返回一组 Konva 色值。冷色调下：
- `#ff7a45` → `#2e7cf6`（笔刷蓝）
- `#e8453c` → `#2e7cf6`（多边形主色）
- `#f0a868` → `#5ba8ff`（强调）
- `#2a1f1f` → `#ffffff`（锚点描边，在亮底下需要白圈或深圈，按对比择优）

---

## 三、新主题配色（草案）

```css
:root[data-theme='light'] {
  /* 主体 */
  --bg: #f4f7fb;
  --panel: #ffffff;
  --border: #d6e2f0;
  --border-hover: #a9c2de;

  /* 主色（蓝） */
  --primary: #2e7cf6;
  --primary-hover: #4a90ff;
  --primary-light: #5ba0ff;
  --accent: #5ba8ff;
  --danger: #e04b4b;
  --success: #2e7cf6;

  /* 文字 */
  --text: #1a2a3a;
  --text-muted: #6b8299;

  /* 棋盘格（透明背景指示） */
  --checker-a: #f4f7fb;
  --checker-b: #e8eef6;

  /* 分层表面 */
  --surface-1: #ffffff;
  --surface-2: #eef3fa;
  --surface-hover: #e2ebf6;

  /* 渐变 */
  --primary-grad: linear-gradient(180deg, #4a90ff 0%, #2e7cf6 100%);
  --primary-grad-hover: linear-gradient(180deg, #5ba0ff 0%, #2670e6 100%);
  --danger-grad: linear-gradient(180deg, #ec5e5e 0%, #e04b4b 100%);
  --danger-grad-hover: linear-gradient(180deg, #f07070 0%, #d63a3a 100%);

  /* 阴影（白底用更柔的蓝灰阴影） */
  --shadow-1: 0 1px 2px rgba(30, 60, 100, 0.08);
  --shadow-2: 0 4px 14px rgba(30, 60, 100, 0.12);
  --shadow-pop: 0 16px 48px rgba(30, 60, 100, 0.18);
  --glow-primary: 0 0 0 3px rgba(46, 124, 246, 0.25);
}
```

暗色保留 `:root` 默认值不变，作为默认主题。

---

## 四、分步执行计划

> 每一步都可独立提交，互不阻塞。

### Step 1 — 变量重构（无视觉变化）
**目的**：消除硬编码，让暗色主题本身也完全跑在变量上。
- 在 `src/index.css` 暗色 `:root` 中新增 `--border-hover` `--danger-grad` `--danger-grad-hover` `--primary-light`
- 把 §2.2 表格中的硬编码值替换为变量
- 视觉应与现在 100% 一致（diff 仅为变量化）

**预估**：15 分钟

### Step 2 — 新增亮色主题变量
- 在 `src/index.css` 暗色 `:root` 之后新增 `:root[data-theme='light'] { ... }` 整块（§三）
- 暂不接入切换 UI，临时手动给 `<html>` 加 `data-theme="light"` 验证视觉
- 调色：在浏览器里逐项目击穿（按钮 / 输入框 / 侧栏 / 工具栏 / 棋盘格）

**预估**：30–40 分钟（大头在调色）

### Step 3 — 主题状态与切换
- `src/stores/` 下新增 `themeStore.ts`（zustand）：`theme: 'dark' | 'light'`，`toggleTheme()`
- 持久化到 `localStorage`（key：`clearcut-theme`），初始化时同步到 `document.documentElement.dataset.theme`
- 订阅 store 变化，写回 `dataset.theme`
- 在 `Toolbar` 右侧加一个图标按钮（太阳 / 月亮），调用 `toggleTheme`

**预估**：30 分钟

### Step 4 — Konva 画布适配
- 新增 `src/hooks/useThemeColors.ts`，按主题返回：
  ```ts
  { brush: string; polyFill: string; polyStroke: string;
    anchorFirst: string; anchorOther: string; anchorStroke: string; }
  ```
- 改写 `BrushLayer.tsx` 与 `PolygonLayer.tsx`，把硬编码色换为 hook 返回值
- 在亮主题下肉眼复核：笔刷描画是否清晰、多边形锚点是否可见

**预估**：30–40 分钟

### Step 5 — 回归验证
- 暗色主题：所有面板 / 按钮 / 输入 / 滑块 / 滚动条 / 画布与改动前一致
- 亮色主题：检查
  - 文字对比度 ≥ 4.5:1（标题、正文、muted 文本）
  - hover / focus / active 状态明显可辨
  - 选中态（`button.active`）在白底下不刺眼
  - 棋盘格（透明指示）在白底下依然可辨
  - 弹层 / 下拉的阴影柔和不漂浮
- 刷新页面后主题保持（localStorage 生效）

**预估**：20 分钟

---

## 五、总工作量与风险

| 项 | 工时 |
|---|---|
| Step 1 变量重构 | 0.25h |
| Step 2 亮色变量与调色 | 0.5–0.7h |
| Step 3 切换 UI + 持久化 | 0.5h |
| Step 4 Konva 适配 | 0.5–0.7h |
| Step 5 回归 | 0.3h |
| **合计** | **约 2–2.5 小时** |

**主要风险**：
1. **Konva 颜色与 CSS 解耦**：JS 拿不到 CSS 变量实时值（能拿但每帧 `getComputedStyle` 不划算），所以走 hook + store 的方式，是本方案唯一非纯 CSS 的位置。
2. **调色主观**：亮色配色草案需要在真实界面上微调，文档给的色值只是起点，不保证一次到位。
3. **第三方图标 / 截图导出**：若导出图片包含 UI 元素，需确认导出逻辑不假设某种主题色（当前看是导出画布内容，与 UI 主题无关，应无影响）。

---

## 六、未来扩展位（不在本期）

- 跟随系统主题（`prefers-color-scheme`）
- 第三套主题（如纯黑 OLED）—— 只需再加一段 `:root[data-theme='xxx']`
- 用户自定义主色 —— 把 `--primary` 暴露到设置面板

---

**确认无误后，按 Step 1 → 5 顺序执行。**
