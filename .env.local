# 本地开发(pnpm dev)也生效。与 .env.production 一致：LaMa 走官方 HuggingFace，
# 这样即使本地没有 public/models/lama 的 208MB 权重，也能直接加载。
# 必须用官方 huggingface.co（hf-mirror 的 308 跳转不带 CORS 头，浏览器会拦）。
# 注意：本文件已纳入 git（见 .gitignore 的 !.env.local 例外）；内容均为 VITE_ 公开变量，无密钥。
VITE_LAMA_MODEL_URL=https://huggingface.co/Carve/LaMa-ONNX/resolve/main/lama_fp32.onnx
