# 本地开发(pnpm dev)也生效。与 .env.production 一致：LaMa 走 hf-mirror，
# 这样即使本地没有 public/models/lama 的 208MB 权重，也能直接从镜像加载。
# 注意：本文件已纳入 git（见 .gitignore 的 !.env.local 例外）；内容均为 VITE_ 公开变量，无密钥。
VITE_LAMA_MODEL_URL=https://hf-mirror.com/Carve/LaMa-ONNX/resolve/main/lama_fp32.onnx
