# 生产构建（vite build）时生效。
# LaMa 权重(208MB)直接指向 HuggingFace 官方，不绑卡、零托管。
# 必须用官方 huggingface.co：hf-mirror 是 308 跳转回官方，但其重定向响应不带
# Access-Control-Allow-Origin，CORS 链在第一跳就被浏览器拦（COEP require-corp）。
# 官方整条重定向链都带 ACAO，可正常加载。
# VITE_LAMA_MODEL_URL 优先级最高，跳过本地/CDN 探测（见 lamaConfig.ts）。
# ORT wasm 与 SAM 随站点同源部署，不需要外部源，故不再配 VITE_ASSET_CDN。
VITE_LAMA_MODEL_URL=https://huggingface.co/Carve/LaMa-ONNX/resolve/main/lama_fp32.onnx
