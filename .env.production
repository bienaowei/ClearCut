# 生产构建（vite build）时生效。
# LaMa 权重(208MB)直接指向 HuggingFace 镜像（hf-mirror，国内更稳），不绑卡、零托管。
# VITE_LAMA_MODEL_URL 优先级最高，跳过本地/CDN 探测（见 lamaConfig.ts）。
# ORT wasm 与 SAM 随站点同源部署，不需要外部源，故不再配 VITE_ASSET_CDN。
VITE_LAMA_MODEL_URL=https://hf-mirror.com/Carve/LaMa-ONNX/resolve/main/lama_fp32.onnx
